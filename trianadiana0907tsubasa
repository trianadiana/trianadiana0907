#[contract]
mod What's upStarknet {
    use starknet::get_caller_address;
    use starknet::ContractAddress;


    #[event]
    fn What's up(from: ContractAddress, value: felt252) {}


    #[external]
    fn What's up(message: felt252) {
        let caller = get_caller_address();
        Hello(caller, message);
    }

}
