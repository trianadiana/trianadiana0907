#[contract]
mod okayStarknet {
    use starknet::get_caller_address;
    use starknet::ContractAddress;


    #[event]
    fn okay(from: ContractAddress, value: felt252) {}


    #[external]
    fn okay(message: felt252) {
        let caller = get_caller_address();
        Hello(caller, message);
    }

}
